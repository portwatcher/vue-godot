name: Godot Smoke

on:
  pull_request:
    paths:
      - '.github/workflows/godot-smoke.yml'
      - 'apps/html-demo/**'
      - 'package*.json'
      - 'packages/**'
      - 'scripts/smoke-editor-reload.mjs'
      - 'scripts/smoke-generated-godot.mjs'
      - 'scripts/smoke-godot.mjs'
      - 'scripts/smoke-utils.mjs'
      - 'turbo.json'
  push:
    branches:
      - develop
      - main
    paths:
      - '.github/workflows/godot-smoke.yml'
      - 'apps/html-demo/**'
      - 'package*.json'
      - 'packages/**'
      - 'scripts/smoke-editor-reload.mjs'
      - 'scripts/smoke-generated-godot.mjs'
      - 'scripts/smoke-godot.mjs'
      - 'scripts/smoke-utils.mjs'
      - 'turbo.json'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  godot-smoke:
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup GodotJS
        uses: ./.github/actions/setup-godotjs

      - name: Install virtual display
        run: sudo apt-get update && sudo apt-get install -y xvfb

      - name: Install
        run: npm ci

      - name: Build
        run: npm run build

      - name: Godot smoke
        run: npm run smoke:godot

      - name: Generated Godot smoke
        run: npm run smoke:generated-godot

      - name: Editor reload smoke
        run: xvfb-run -a --server-args="-screen 0 1280x720x24" npm run smoke:editor-reload
